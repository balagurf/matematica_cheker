<Window x:Class="GrammarChecker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Grammar Type Checker" Height="520" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Вставьте правила граматики (по одному правилу на строку, формат: A->aB). Допустимо: 'λ', 'Λ', 'eps', или пустая правая часть — как ε."
                   TextWrapping="Wrap" FontWeight="Bold" />

        <TextBox x:Name="RulesInput" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="14" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,8">
            <Button x:Name="CheckButton" Content="Анализировать грамматику" Padding="10,6"
                    Click="CheckButton_Click"/>
            <Button Content="Очистить" Margin="10,0,0,0" Padding="10,6" Click="ClearButton_Click"/>
            <TextBlock x:Name="StatusText" Margin="16,4,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <TextBox x:Name="DetailsOutput" Grid.Row="3" IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13" TextWrapping="Wrap"/>
    </Grid>
</Window>
